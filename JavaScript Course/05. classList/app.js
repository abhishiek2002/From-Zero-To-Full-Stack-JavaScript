// classList
// add()
// remove()
// toggle()

var h1 = document.querySelector("h1");

// console.log(h1.classList);  // list all the classes h1 has

// // we will add class to the h1 so that ".styles" class css work on it

// h1.classList.add("styles"); // add class to the element


// // you can remove class also
// h1.classList.remove("styles");  // remove class from the element

// // toggle remove class if class is present or add class if it is not present.

// h1.classList.toggle("apple");  // if "apple" is not a class of h1 element then toggle() will add it and if "apple" is a class of h1 element then toggle() will remove it.
