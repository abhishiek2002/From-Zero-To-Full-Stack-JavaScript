// gsap.methodName(elementName, vars obj);

// to method : - 

gsap.to(".box", {
    // x:100,  // translate ".box" element to 100px on x axis
    duration:2, // 2sec duration
    y:150,

    // repeat:-1  // repeat infinite time
    repeat:2,
});
